properties([disableConcurrentBuilds])

pipeline {
    agent { label 'master' }
    stages {
        stage('Preparations') {
            steps {
                sh "ssh root@localhost:8080"
            }
        }
}
